'use server'

import { env } from "@/env.mjs"
import {redirect} from "next/navigation";


export async function SendUuidAPI(uuid: string)  {

    // const response = await fetch(env.SERVER_API_URL +'/api/uuid-auth/' + uuid, {
    // // // const response = await fetch('/api/validate-email/' + uuid, {
    //     method: 'PUT'
    // });
    // redirect("/api/validate-email/" + uuid);
// const email = "email";
//
//
//     if(response.status === 200) {
//
//     }

}